import type { Option } from "@/types/content";

/**
 * Budget ranges for the consultation form.
 * CONTENT INPUT REQUIRED (Q-6): ranges and currency. While empty, the budget
 * field is hidden from the form.
 */
export const budgetOptions: Option[] = [];
